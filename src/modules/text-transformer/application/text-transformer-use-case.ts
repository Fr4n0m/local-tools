import {
  capitalize,
  lowercase,
  trim,
  uppercase,
} from "@/modules/text-transformer/domain/transform-text";

export const textTransformerUseCase = {
  uppercase,
  lowercase,
  capitalize,
  trim,
};
